version: '3.9'

services:
  frontend:
    container_name: myapp-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:80'
    restart: unless-stopped
    environment:
      NODE_ENV: production
